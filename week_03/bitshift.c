#include <stdio.h>

int main()
{
    int experiment = 1;
    //bitshifting
    printf("Bitshift left: %d\n", experiment << 3);  // Whats going on?
    printf("Bitshift right: %d\n", experiment >> 3); // Whats going on?
    return 0;
}